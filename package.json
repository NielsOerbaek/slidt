{
  "name": "slidt",
  "version": "0.0.0",
  "private": true,
  "description": "Slide webservice — core renderer + template system",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "postcss": "^8.4.35"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.2",
    "vitest": "^1.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
